Here is a list of users Mr. Admin:
<ul class="admin_users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.user_name, admin_user_path(user) %>
    </li>
  <% end %>
</ul>
<%= will_paginate @users %>
